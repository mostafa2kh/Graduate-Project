# Infrastructure

This folder contains infrastructure-related configuration files:

- Deployment scripts
- Nginx configuration (future)
- Dockerfiles for individual services (future)
- CI/CD workflow files (future)
- Backup scripts (future)

## Current Status

Phase 0: Infrastructure folder created as placeholder.
Docker Compose infrastructure is in the root `docker-compose.yml` file.
